# zorn's factory UDFPS calibration is kept in
# /mnt/vendor/persist/fingerprint.  qseecomd (the tee domain) loads it
# directly before Goodix can begin enrollment.
allow tee mnt_vendor_file:dir { create_dir_perms relabelto };
allow tee mnt_vendor_file:file { create_file_perms relabelto };
