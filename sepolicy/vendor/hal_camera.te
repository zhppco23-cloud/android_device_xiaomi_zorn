# The camera provider enables CCI I/O protection when it starts a video
# pipeline.  Without this, VideoEIS2 fails to return its output buffers.
allow hal_camera_default vendor_sysfs_camera_cci:file w_file_perms;
